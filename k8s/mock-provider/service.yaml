apiVersion: v1
kind: Service
metadata:
  name: mock-provider
  namespace: proxy-system
  labels:
    app: mock-provider
spec:
  type: ClusterIP
  selector:
    app: mock-provider
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
